using UnityEngine;

public class NewMonoBehaviourScript : MonoBehaviour
{
    private const int qwe = 10;

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        int a;

        a = qwe;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
